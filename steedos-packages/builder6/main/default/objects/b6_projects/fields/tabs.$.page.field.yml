name: tabs.$.page
amis:
  requiredOn: '${type == "page"}'
group: 菜单
hidden: false
label: 页面
reference_to: b6_pages
required: false
sort_no: 170
type: lookup
visible_on: '${type == "page"}'
filters: ["project_id", "=", "${_master.recordId}"]
auto_fill_mapping:
  - from: url
    to: url
  - from: name
    to: name
